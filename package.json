{
    "name": "athena-monorepo",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix frontend\" \"cd backend && python -m uvicorn app.main:app --reload\"",
        "start": "npm run dev",
        "lint": "oxlint --config oxlint.config.mjs frontend/src frontend/tests sdk/typescript/src sdk/typescript/tests"
    },
    "devDependencies": {
        "@oxlint/plugins": "1.78.0",
        "concurrently": "^8.2.2",
        "node": "22.18.0",
        "oxlint": "1.78.0"
    }
}
